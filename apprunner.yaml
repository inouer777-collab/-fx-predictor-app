version: 1.0
runtime: python3
build:
  command: python aws_fx_phase1.py
    build:
      - pip install -r requirements.txt
run:
  command: python aws_fx_simple.py
  network:
    port: 8443
    env: PORT
