version: 1.0
runtime: python3
build:
  commands:
    build:
      - echo "依存関係をインストールしています..."
      - pip install -r requirements.txt
run:
  command: python aws_fx_app_final.py
  network:
    port: 8443
    env: PORT
